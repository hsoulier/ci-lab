name: Build
description: Execute build for Cypress or Deploy to Firebase Hosting

# inputs:
#   secrets:
#     description: "Secrets to use in the build"
#     required: true

runs:
  using: composite
  steps:
    - name: Debug values
      run: |
        echo "Secrets: ${{ env.secrets }}"
        echo "Secret 1: ${{ env.secrets.secret1 == 'secret1' }}"
        echo "Secret 2: ${{ env.secrets.secret2 == 'secret2' }}"
      shell: bash
    - name: 🚚 Build
      run: yarn build
      shell: bash
