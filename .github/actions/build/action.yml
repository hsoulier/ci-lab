name: Build
description: Execute build for Cypress or Deploy to Firebase Hosting

runs:
  using: composite
  steps:
    - name: Debug values
      run: |
        echo "Secrets: ${{ env.secrets }}"
        echo "Secret 1: ${{ env.secrets['secret1'] }}"
        echo "Secret 2: ${{ env.secrets['secret1'] }}"
      shell: bash
    - name: 🚚 Build
      run: yarn build
      shell: bash
